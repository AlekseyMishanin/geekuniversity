package javacoreprofession.lesson5.additional.model;

/**
 * Перечисление описывающее возможные статусы порта
 * @author Mishanin Aleksey
 * */
public enum StatusPort {
    ACTIVE,         //активный(склад не пустой)
    INACTIVE;       //не активный(склад пуст)
}
